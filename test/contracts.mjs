import test from 'node:test';import assert from 'node:assert/strict';import {createYournotifyClient} from '../src/index.js';
test('canonical namespaces and routes',async()=>{const calls=[];globalThis.fetch=async(url,options)=>{calls.push([options.method,new URL(url).pathname,options.body?JSON.parse(options.body):null]);return{ok:true,status:200,json:async()=>({})}};const sdk=createYournotifyClient({apiKey:'test'});await sdk.voice.send({name:'Voice'});await sdk.inapp.send({name:'Inapp'});await sdk.lists.export(4);await sdk.trackBatch([{event:'order.completed'}]);assert.deepEqual(calls.map(([method,path])=>[method,path]),[['POST','/campaigns/voice'],['POST','/campaigns'],['GET','/lists/export/4'],['POST','/automations/events/batch']]);assert.ok(calls[3][2].events[0].event_id);assert.ok(calls[3][2].events[0].occurred_at);});
